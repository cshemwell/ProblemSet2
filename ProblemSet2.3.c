#include <stdio.h>
#include <stdbool.h>
#include <math.h>

int main(int argc, char const *argv[]) {

    int A, B;
    int a = 10;
    int b = 20;

    A = a;
    B = b;



printf("A: %d/n, B: %d/n", A, B);

    A = b;
    B = a;

printf("A: %d/n, B: %d/n", A, B);

return 0;
}
